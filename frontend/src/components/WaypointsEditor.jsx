import React from 'react'

export default function WaypointsEditor(){
  return (
    <div>
      <p>Simple waypoint editor placeholder — extend as needed for RNAV legs, inbound/outbound tracks, distances, etc.</p>
    </div>
  )
}
