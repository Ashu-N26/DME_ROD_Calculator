import React from 'react'

export default function GraphProfile({profile}){
  return (
    <div>
      <p>Profile graph placeholder — implement plotting (e.g., using chart libraries) if desired.</p>
    </div>
  )
}
