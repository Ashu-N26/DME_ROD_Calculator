services:
  - type: web
    name: dme-rod-calculator
    env: python
    buildCommand: pip install -r backend/requirements.txt && npm ci --prefix frontend && npm run build --prefix frontend
    startCommand: gunicorn backend.wsgi:app -b 0.0.0.0:10000
